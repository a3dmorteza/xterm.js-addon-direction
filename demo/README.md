# Direction Addon Demo

This demo shows how to use the Direction addon for xterm.js to switch between RTL (Right-to-Left) and LTR (Left-to-Right) text directions.

## Running the Demo

1. First, build the addon:
   ```bash
   npm run package